对象类，即类带属性或接口

一组对象类与一组方法映射，可能：
0102  一个对象类拥有一个toolkit(visitor)类中的一个method，但toolkit有多个可选。

03      一个对象类拥有一个state类中的所有method，state有多个可选且可切换。

04      一个对象类拥有一个strategy类中的所有method，但一个对象类就一个strategy不可选。
